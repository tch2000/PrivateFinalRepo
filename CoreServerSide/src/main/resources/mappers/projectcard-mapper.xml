<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.penta.seungbae.proejctcard">
	<!-- 
	private ProjectVO projectVO;
	private RequiredSkillVO requiredSkillVO;
	private List<ManageProjectPartVO> mPPVOList;
	private RecruitProjectVO recruitProjectVO;
 -->
 	
	
	<select id="selectOneProject" resultType="edu.hexa.teamsns.domain.ProjectVO">
		SELECT
			*
		FROM
			project
		WHERE
			pid = #{pid}		
	</select>
	
	<select id="selectProjects" resultType="edu.hexa.teamsns.domain.ProjectVO">
		SELECT
			*
		FROM
			project		
	</select>

	<select id="selectOneRequiredSkills" resultType="edu.hexa.teamsns.domain.RequiredSkillVO">
		SELECT
			*
		FROM
			required_skill
		WHERE
			pid = #{pid}		
	</select>
	
	<select id="selectRequiredSkills" resultType="edu.hexa.teamsns.domain.RequiredSkillVO">
		SELECT
			*
		FROM
			required_skill	
	</select>
	
	<select id="selectManageProjectParts" resultType="edu.hexa.teamsns.domain.ManageProjectPartVO">
		SELECT
			*
		FROM
			manage_project_part
	</select>
	
	
	
	
	
		
<!-- <select id="selectByTeamId" resultType="TeamCardDTO">
		SELECT 
		team_id, team_name, team_intro, team_startday, 
		user_id, team_part, user_profile, recruit_hits
		required_skill_1, required_skill_2, required_skill_3, required_skill_4,
		required_skill_5, required_skill_6, required_skill_7, required_skill_8,
		required_skill_9, required_skill_10
		
		FROM
		team, manage_team_part, required_skill, user, recruit_team
		
		WHERE
		team_id=${team_id}
	</select> -->	

 	
</mapper>  